// Directorio de hooks personalizados
// Coloca aquí los hooks reutilizables de la aplicación, por ejemplo:
// - useAuth.ts     → gestión del estado de autenticación
// - useProducts.ts → obtención de datos con estados de carga y error
// - useDebounce.ts → valores de input con retardo (debounce)
