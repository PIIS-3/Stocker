// Extiende los matchers de Vitest con los de Testing Library (ej: .toBeInTheDocument(), .toHaveClass())
// Esto permite realizar aserciones más expresivas sobre el DOM en las pruebas.
import '@testing-library/jest-dom';
